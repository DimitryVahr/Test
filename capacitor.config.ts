import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nutnet.vityaz',
  appName: 'vityaz',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
